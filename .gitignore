# Build artifacts
build/
*.qmlc
*.jsc
__pycache__/

# Suite launch log (start-suite.ps1 writes it into the repo root)
scratchpad_suite.log

# Stray metadata exports (live in ~/xylosome_exports, not the repo)
xylosome_*.svg

# Home-directory junk that must never be committed
.CFUserTextEncoding
.adobe/
.android/
.arduinoIDE/
.aspera/
.bash_history
.bash_sessions/
.claude.json
.config/
.cups/
.flexlmrc
.lesshst
.oracle_jre_usage/
.ssh/
.wokwi/
.zprofile
.DS_Store

build-suite/

# Per-machine Claude Code settings (personal, not shared)
.claude/settings.local.json
dist/

# macOS suite build (build-suite-mac.sh)
build-suite-mac/
build-suite-mac.log
